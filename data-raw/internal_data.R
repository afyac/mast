## code to prepare `internal_data` dataset goes here
# Actively adding to internal dictionaries


# What are we alwasy saving
usethis::use_data(som_admins, causes_of_death, overwrite = TRUE, internal = TRUE)
